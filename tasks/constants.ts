// Centralized constants for task scripts

// Networks where demo/destructive flows should never run
export const DISALLOWED_NETWORKS = new Set<string>(['mainnet', 'polygon', 'polygonMainnet', 'base', 'gnosis']);
